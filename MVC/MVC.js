define(['./record', './model', './EJS', './controller'], function(record, model, EJS, controller){
 'use strict';
  return {
    Record: record,
    Model: model,
    EJS: EJS,
    Controller: controller
  }
})